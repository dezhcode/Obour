# بررسی انطباق با مستندات رسمی PasarGuard

کد با مستندات رسمی SDK نسخه ۲.۴.۰ (آخرین نسخه، ۲۶ اوت ۲۰۲۶) مقایسه شد.
چند مشکل جدی پیدا و اصلاح شد.

---

## ۱. نام متدها اشتباه بود (بحرانی)

کد قبلی متدهایی صدا می زد که در SDK رسمی **وجود ندارند**. ربات موقع
اولین خرید کرش می کرد.

| کد قبلی (اشتباه) | متد رسمی SDK | وضعیت |
|---|---|---|
| `get_user_by_username()` | `get_user()` | ✅ اصلاح شد |
| `modify_user_by_username()` | `modify_user(username=, user=)` | ✅ اصلاح شد |
| `remove_user_by_username()` | `remove_user()` | ✅ اصلاح شد |
| `reset_user_data_usage_by_username()` | در مستندات عمومی نیست | ✅ با fallback جایگزین شد |

برای صفر کردن مصرف، چند نام محتمل امتحان می شود و در نهایت درخواست
مستقیم HTTP زده می شود، چون این متد در مستندات عمومی نیامده.

---

## ۲. احراز هویت (بحرانی)

مستندات رسمی می گوید SDK با **Bearer token** کار می کند:

```python
token = await api.get_token(username=..., password=...)
user = await api.create_user(UserCreate(...), token=token.access_token)
```

کد قبلی فقط `api_key` پاس می داد که در امضای رسمی `PasarguardAPI` وجود ندارد.

**حالا هر دو روش پشتیبانی می شود:**

```env
# روش ۱ (طبق مستندات رسمی)
PANEL_USERNAME=admin
PANEL_PASSWORD=yourpassword

# روش ۲ (اگر بیلد SDK پشتیبانی کند)
PANEL_API_KEY=...
```

کد با `inspect.signature` چک می کند آیا SDK از `api_key` پشتیبانی می کند؛
اگر نه، هدر `X-API-Key` را دستی روی کلاینت می گذارد. اگر یوزر/پسورد داده
شده باشد، توکن گرفته و کش می شود و به همه فراخوانی ها پاس داده می شود.

---

## ۳. استفاده از متد رسمی برای همه گروه ها

مستندات نشان می دهد SDK یک متد آماده دارد که دقیقا کار مورد نیاز ما را
انجام می دهد:

```python
user = await api.create_user_in_all_groups(UserCreate(...), token=...)
```

**حالا:** اگر `PANEL_GROUP_ID=all` باشد و این متد در SDK موجود باشد، از
همان استفاده می شود (تمیزتر و مطمئن تر). اگر نبود، به روش قبلی برمی گردد:
خواندن لیست گروه ها و پاس دادن `group_ids`.

برای خواندن گروه ها هم اول متد رسمی `get_all_groups()` امتحان می شود،
بعد اندپوینت `/api/groups/simple` (دسترسی اپراتور)، بعد `/api/groups` (sudo).

---

## ۴. مواردی که از قبل درست بودند ✅

- `create_user(UserCreate(...))` — دقیقا مطابق مستندات
- `data_limit` بر حسب بایت — درست
- `expire=Tools.days(30)` — درست (timestamp یونیکس)
- `group_ids=[...]` لیست — درست
- `status=UserStatus.ACTIVE` — درست
- `note` — درست
- مدیریت خطا با `httpx.HTTPStatusError` — دقیقا مطابق بخش Error Handling مستندات
- `get_system_stats()` برای health check — درست
- نسخه `pasarguard>=2.4.0` در requirements — درست

---

## نکته مهم برای اجرا

چون متد `reset_user_data_usage` در مستندات عمومی نیامده، **حتما یک تمدید
آزمایشی انجام بده** و چک کن که مصرف قبلی صفر شده باشد. اگر نشد، لاگ ربات
نام درست متد را نشان می دهد و می توان اصلاحش کرد.

همچنین اولین کاری که بعد از راه اندازی باید بکنی:
1. یک خرید آزمایشی با مبلغ کم
2. چک کردن لینک ساب در اپ
3. زدن دکمه «🌐 گروه های پنل» در پنل ادمین ربات
