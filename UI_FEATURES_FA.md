# قابلیت های جدید رابط کاربری

شش مورد اعمال شد. همه با تشخیص خودکار نسخه: اگر aiogram پشتیبانی نکند
بی سروصدا کنار گذاشته می شوند و ربات کار می کند.

---

## ۱. دکمه های رنگی (Bot API 9.4)

| رنگ | کاربرد |
|-----|--------|
| 🟢 سبز `success` | تست رایگان، تایید خرید، شارژ، تمدید |
| 🔵 آبی `primary` | خرید سرویس، لینک و QR، دعوت هم سفر |
| 🔴 قرمز `danger` | انصراف، حذف، مسدودسازی |
| ⚪ خنثی | برگشت، منوی اصلی |

رنگ چشم کاربر را به سمت اقدام اصلی می برد و نرخ تبدیل را بالا می برد.

## ۲. ایموجی سفارشی روی دکمه

نیازمند Telegram Premium برای صاحب ربات. در فایل env:
```env
ICON_BUY=5774022692642492953
ICON_WALLET=
ICON_GIFT=
ICON_OK=
```
برای گرفتن آیدی: ایموجی پریمیوم را به ربات بفرست و `custom_emoji_id` را از لاگ بردار.
اگر خالی بماند، دکمه بدون ایموجی نمایش داده می شود.

## ۳. دکمه غیرفعال

در ساخت دلخواه، نمایش حجم و مدت حالا دکمه نمایشی است نه دکمه بی اثر با
callback. تمیزتر و بدون درخواست بی مورد به سرور.

## ۶. نشانگر تایپینگ

هنگام ساخت سرویس و تمدید، `sendChatAction` فرستاده می شود تا کاربر
بفهمد ربات زنده است. تابع `ui.working()`.

## ۷. دعوت با switch_inline_query

دکمه دعوت هم سفر حالا لیست چت های کاربر را همان جا باز می کند و متن دعوت
را آماده می فرستد — به جای باز شدن مرورگر. دکمه کپی لینک هم کنارش هست.

## ۸. بازخورد سریع (toast)

موقع تغییر اسلایدر حجم یا مدت در ساخت دلخواه، قیمت جدید فوری روی خود
دکمه نمایش داده می شود. تابع `ui.toast()`.

---

## بهبود متن ها

- لینک ساب، شماره کارت و مبلغ داخل `<code>` — یک لمس = کپی
- هشدار مبلغ دقیق داخل `<blockquote>` — نوار کناری تمیز
- نوار پیشرفت متنی برای مصرف
- فاصله گذاری تنفس دار بین بخش ها

## تحویل یکپارچه سرویس

قبلا QR و متن دو پیام جدا بودند. حالا تابع `ui.deliver()` عکس و متن و
دکمه ها را یک پیام واحد می کند. اگر ساخت QR شکست بخورد، متن به تنهایی
فرستاده می شود تا کاربر بدون لینک نماند.

---

## QR سفارشی (زیرساخت آماده، منتظر تصویر)

کد آماده است. کافی است تصویر قاب را بسازی و در `assets/qr_frame.png`
بگذاری، بعد در env تنظیم کنی:

```env
QR_FRAME_PATH=assets/qr_frame.png
QR_BOX_X=100        # فاصله از چپ تا محل QR
QR_BOX_Y=280        # فاصله از بالا تا محل QR
QR_BOX_SIZE=600     # اندازه مربع QR
QR_FG=#0a1e3f
QR_BG=#ffffff
```

**پرامپت ساخت تصویر:**
```
Vertical mobile-style card design, dark navy blue gradient background,
glowing blue accents, minimalist tech aesthetic. Empty white rounded
square area in the center reserved for a QR code. Clean space at top
for a logo and space at bottom for text. Monochrome blue palette only,
no purple. No text, no QR code, just the frame design. Ratio 4:5.
```

راهنمای کامل در `assets/README_FA.md`.

---

## نکته مهم برای تست

رنگ دکمه ها و ایموجی سفارشی فقط با aiogram نسخه ۳.۲۵ به بالا دیده می شوند.
اگر رنگ ها را ندیدی:
```bash
pip install -U "aiogram>=3.25"
```
