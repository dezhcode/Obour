# راهنمای راه اندازی Obour

## قدم ۱: ساخت فایل env

```bash
cp .env.example .env
nano .env      # یا هر ویرایشگر دیگر
```

## قدم ۲: پر کردن مقادیر

مقادیر پنل تو از قبل پر شده اند:

```env
PANEL_BASE_URL=https://zerofee.ok-exor.ir
PANEL_USERNAME=salatinvpn
PANEL_PASSWORD=<<< اینجا پسورد پنلت را بنویس >>>
PANEL_GROUP_ID=all
```

### درباره آدرس پنل
آدرس داشبوردت `https://zerofee.ok-exor.ir/dashboard/` است، ولی کد به
**آدرس پایه** نیاز دارد یعنی بدون `/dashboard`. این از قبل درست تنظیم شده.
(اگر اشتباهی با dashboard وارد کنی، کد خودش پاکش می کند.)

### درباره پسورد
- پسورد را **فقط** در فایل `.env` روی سرور خودت بنویس.
- هرگز در چت، پیام، اسکرین شات یا گیت قرار نده.
- فایل `.env` در `.gitignore` هست، پس در گیت آپلود نمی شود.
- اگر جایی لو رفت، فورا از پنل عوضش کن.

مجوز فایل را محدود کن تا فقط خودت بخوانی:
```bash
chmod 600 .env
```

### بقیه مقادیری که باید پر کنی
```env
BOT_TOKEN=        # از @BotFather
ADMIN_IDS=        # آیدی عددی خودت، از @userinfobot
```

## قدم ۳: نصب و اجرا

```bash
pip install -r requirements.txt
python seed_plans.py     # درج پلن ها در دیتابیس
python main.py           # اجرا (حالت polling برای تست)
```

## قدم ۴: تست اولیه (مهم)

به ترتیب این ها را چک کن:

1. **اتصال به پنل:** لاگ موقع بالا آمدن باید بگوید `panel health: OK`.
   اگر FAILED بود یعنی آدرس یا یوزر/پسورد اشتباه است.

2. **گروه ها:** در ربات `/start` بزن، برو پنل ادمین → تنظیمات →
   **🌐 گروه های پنل**. باید لیست گروه ها را نشان دهد. اگر خالی بود،
   یعنی دسترسی کافی نداری یا گروهی در پنل نیست.

3. **شماره کارت:** از پنل ادمین ربات، شماره کارت و نام صاحب کارت را ثبت کن.

4. **خرید آزمایشی:** موجودی خودت را دستی شارژ کن و یک پلن ارزان بخر.
   لینک ساب را در اپ (v2rayNG یا Hiddify) تست کن که کانفیگ ها بیایند.

5. **تمدید آزمایشی:** همان سرویس را تمدید کن و چک کن مصرف قبلی صفر شده باشد.

## قدم ۵: انتقال به پروداکشن (webhook)

بعد از اینکه همه چیز در حالت polling کار کرد:

```env
WEBHOOK_MODE=true
WEBHOOK_BASE_URL=https://your-domain.com
WEBHOOK_SECRET=<<< یک رشته تصادفی طولانی >>>
```

برای ساخت رشته تصادفی:
```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

---

## اگر مشکلی پیش آمد

- **panel health: FAILED** → آدرس پنل یا یوزر/پسورد را چک کن. مطمئن شو
  آدرس بدون `/dashboard` است.
- **گروه ها خالی** → با دسترسی sudo وارد شو یا از مدیر پنل بخواه دسترسی
  خواندن گروه ها را بدهد.
- **سرویس ساخته می شود ولی کانفیگ ندارد** → یعنی گروه درست ست نشده.
  دکمه «گروه های پنل» را چک کن.
- **خطای متد ناشناخته** → نسخه SDK را به روز کن:
  `pip install -U pasarguard`
