# پرداخت کریپتو (TON و USDT) با TON Connect

برای کاربرهایی که بیرون از ایران هستند و کارت بانکی ایران ندارند. کاربر
کیف پول تومانی ربات را با TON یا تتر (USDT روی شبکه TON) شارژ می کند و
بعد مثل همیشه خرید می کند. تایید **خودکار** است؛ ادمین لازم نیست.

## چطور کار می کند

1. کاربر مبلغ را به تومان و ارز را انتخاب می کند. یک فاکتور با کد یکتا
   (مثل `OB-7K2M9Q`) ساخته می شود و نرخ همان لحظه روی آن قفل می شود.
2. پرداخت:
   - **مینی اپ:** کیف پول (Tonkeeper، Telegram Wallet، MyTonWallet و…) با
     TON Connect وصل می شود و تراکنش را امضا می کند. همه چیز خودکار پر می شود.
   - **ربات:** دکمه «پرداخت با Tonkeeper» (لینک از پیش پر شده)، یا
     پرداخت دستی با آدرس + مبلغ + کامنت.
3. ربات تراکنش های ورودی کیف پول شما را از Toncenter می خواند، کد فاکتور
   را در کامنت پیدا می کند و اگر مبلغ کافی بود، کیف پول کاربر را شارژ و
   به خودش و ادمین ها خبر می دهد.

نکته های امنیتی:
- ربات فقط **آدرس** کیف پول را لازم دارد، نه کلید خصوصی.
- چیزی که مینی اپ بعد از پرداخت می گوید مبنای شارژ نیست؛ فقط تراکنش
  واقعی روی زنجیره.
- تتر از کیف پول جتونِ خودِ شما خوانده می شود که آدرسش از خود قرارداد
  تتر پرسیده شده؛ جتون تقلبی با اسم USDT شارژی نمی سازد.
- هر تراکنش فقط یک بار حساب می شود.
- کم پرداخت خودکار شارژ نمی شود و برای ادمین گزارش می شود.
- اگر کاربر بعد از «انصراف» یا بعد از مهلت پرداخت کرد، تا ۴۸ ساعت پولش
  شناخته و شارژ می شود.

## راه اندازی روی testnet (برای تست)

در `.env`:

```
TON_NETWORK=testnet
TON_RECEIVE_ADDRESS=آدرس کیف پول شما
TONCENTER_API_KEY=کلید testnet از @tonapibot
```

بعد Restart. در پنل ادمین: تنظیمات ← «💵 نرخ تتر» (مثلا 102000) و اگر
خواستی «💎 نرخ TON» (۰ یعنی خودکار از قیمت دلاری TON × نرخ تتر).

تست TON:
1. در Tonkeeper حالت تست نت را روشن کن (Settings → چند بار روی لوگو بزن
   → Dev Menu → Switch to Testnet) یا یک کیف پول تست نت جدا بساز.
2. از [@testgiver_ton_bot](https://t.me/testgiver_ton_bot) TON تستی بگیر.
3. در ربات: کیف پول ← «💎 پرداخت با TON / USDT»، یا در مینی اپ: شارژ کیف
   پول ← «TON / USDT».

تست USDT روی testnet: تتر رسمی روی testnet وجود ندارد. در
[minter.ton.org](https://minter.ton.org/?testnet=true) یک جتون تستی با
**۶ رقم اعشار** بساز (توکن ها به کیف پول خودت می آید)، آدرس مستر آن را در
`TON_USDT_MASTER` بگذار و Restart کن.

بهتر است برای پرداخت از یک کیف پول دوم استفاده کنی، نه همان کیف پول دریافت.

## رفتن روی mainnet

```
TON_NETWORK=mainnet
TON_RECEIVE_ADDRESS=آدرس کیف پول اصلی
TONCENTER_API_KEY=کلید mainnet از @tonapibot
TON_USDT_MASTER=           # خالی: تتر رسمی
```

کیف پول دریافت را یک بار با یک تراکنش خروجی فعال کن (Tonkeeper خودش با
اولین ارسال فعالش می کند). دریافت TON و تتر روی کیف پول فعال نشده هم
کار می کند، ولی فعال بودن مطمئن تر است.

## فایل ها

- `app/ton.py` — آدرس و BOC بدون کتابخانه بیرونی
- `app/services/crypto.py` — فاکتور، نرخ، خواندن Toncenter، تطبیق و شارژ
- `app/handlers/wallet.py` — گفتگوی ربات (`cw:*`)
- `app/webapp/api.py` و `wsgi.py` — `crypto`، `crypto/start|pay|status|cancel`
  و `/tonconnect-manifest.json`
- `app/webapp/static/tonconnect-ui.min.js` — نسخه 3.0.2، روی خود سرور
