# مینی اپ عبور — فاز صفر و یک

مکمل ربات است، نه جایگزین. همه دستورها و دکمه های فعلی ربات دقیقا سر
جایشان می مانند. مینی اپ در این فاز **فقط خواندنی** است: هر کاری که
پول یا داده را تغییر می دهد (خرید، شارژ، تمدید، تیکت) کاربر را به
همان مسیر آزموده در ربات می فرستد.

---

## فایل های تازه

```
app/webapp/__init__.py          آدرس و کلید روشن/خاموش
app/webapp/auth.py              احراز هویت initData (HMAC)
app/webapp/api.py               اندپوینت های خواندنی
app/webapp/wsgi.py              پل بین Passenger و توابع async
app/webapp/static/index.html    خود مینی اپ (یک فایل، بدون CDN)
```

## فایل های تغییر کرده

```
passenger_wsgi.py   مسیر /app اضافه شد + ثبت خودکار دکمه منوی چت
app/keyboards.py    دکمه «🪟 مینی اپ عبور» در منوی اصلی
```

---

## ۱) env

به `.env` اضافه کن:

```ini
# مسیر مینی اپ نسبت به ریشه اپ (پیش فرض app)
WEBAPP_PATH=app
# خاموش کردن کامل مینی اپ بدون حذف کد
WEBAPP_ENABLED=true
# حداکثر عمر initData به ثانیه (پیش فرض ۶ ساعت)
WEBAPP_MAX_AGE=21600
```

و مطمئن شو این مقدار **دقیقا** همین است:

```ini
WEBHOOK_BASE_URL=https://dezhcode.pyho.ir/obour
APP_BASE_URI=/obour
```

اگر `WEBHOOK_BASE_URL` با `https://` شروع نشود، مینی اپ خودش را خاموش
می کند و دکمه اش ساخته نمی شود — چون تلگرام مینی اپ روی http را باز
نمی کند و دکمه ای که کار نکند از نبودنش بدتر است.

## ۲) سی پنل

در Setup Python App:

```
Application root         obour            (مسیر فایل ها روی هاست)
Application URL          dezhcode.pyho.ir/obour
Application startup file passenger_wsgi.py
Application entry point  application
```

بعد از هر تغییر فایل، **Restart** را بزن (یا `touch tmp/restart.txt`).

پیش نیاز: AutoSSL روی `dezhcode.pyho.ir` صادر شده باشد. بدون گواهی
معتبر، تلگرام صفحه را اصلا باز نمی کند و پیامی هم نشان نمی دهد.

## ۳) فونت (اختیاری ولی توصیه می شود)

فایل `vazirmatn.woff2` را در `app/webapp/static/` بگذار. اگر نگذاری،
صفحه روی فونت سیستم می افتد و چیزی نمی شکند — فقط ظاهرش با ربات یکدست
نمی ماند. هیچ CDN خارجی در کار نیست؛ این عمدی است.

## ۴) ثبت آدرس

یک بار این آدرس را بزن:

```
https://dezhcode.pyho.ir/obour/setwebhook?key=ADMIN_KEY
```

همین یک درخواست سه کار می کند: وبهوک، فهرست دستورها، و **دکمه منوی
چت** (کنار فیلد تایپ) که به مینی اپ وصل می شود. لازم نیست آدرس را
دستی در BotFather ثبت کنی.

خروجی باید این شکلی باشد:

```
webhook set: https://dezhcode.pyho.ir/obour/webhook/obour
webapp: https://dezhcode.pyho.ir/obour/app
```

اگر `webapp: off` دیدی یعنی `WEBHOOK_BASE_URL` یا https نیست یا
`WEBAPP_ENABLED` خاموش است.

---

## آدرس ها

| مسیر | کار |
|---|---|
| `/obour/app/` | خود مینی اپ |
| `/obour/app/api/bootstrap` | کاربر، موجودی، شمارنده ها |
| `/obour/app/api/services` | فهرست سرویس ها با مصرف زنده |
| `/obour/app/api/service?id=N` | جزئیات + لینک ساب + اتصال سریع |
| `/obour/app/api/qr?id=N` | تصویر QR (همان تابع ربات) |
| `/obour/app/api/wallet?offset=0&kind=charge` | سوابق مالی |
| `/obour/app/api/plans` | ویترین فروشگاه |
| `/obour/app/api/referral` | هم سفرها |
| `/obour/app/api/tickets` | فهرست تیکت ها |

هر دو شکل `/obour/app` و `/obour/app/` کار می کنند. مسیر پایه را
سرور داخل HTML تزریق می کند (جای `__BASE__`)، پس صفحه از هر آدرسی
فونت و فایل های static و API را درست پیدا می کند.

---

## امنیت

- **احراز هویت:** هر درخواست API رشته `initData` تلگرام را در هدر
  `X-Init-Data` می آورد و امضایش با `HMAC-SHA256` سنجیده می شود.
  هدر انتخاب شده نه query string، چون query string در access log
  هاست ذخیره می شود و initData تا ۶ ساعت معتبر است.
- **عمر نشست:** `auth_date` قدیمی تر از `WEBAPP_MAX_AGE` رد می شود.
- **مالکیت:** `service?id=` و `qr?id=` قبل از هر چیز چک می کنند که
  سرویس مال همان کاربر باشد. بدون این خط، هر کسی با شماره گذاری
  ساده لینک ساب دیگران را می خواند.
- **فقط خواندنی:** هر متدی جز GET با ۴۰۵ رد می شود.
- **سقف نرخ:** ۶۰ درخواست در دقیقه به ازای هر کاربر تلگرام.
- **CSP:** فقط منابع خودی؛ `frame-ancestors` فقط دامنه های تلگرام
  (نسخه وب تلگرام مینی اپ را داخل iframe باز می کند، پس
  `X-Frame-Options: DENY` آن را می شکند).
- **XSS:** هر متنی که از سرور می آید از `esc()` رد می شود. نام سرویس
  و متن تیکت را خود کاربر نوشته.
- **بدون ثبت نام:** اگر کاربر `/start` نزده باشد، مینی اپ او را به
  ربات می فرستد. پذیرش قوانین و عضویت اجباری دور زده نمی شوند.

---

## کارهایی که این فاز عمدا نمی کند

- **خرید و شارژ.** منطق خرید الان داخل `handlers/buy.py::_do_purchase`
  است. قبل از هر مسیر پرداخت دوم، باید به `app/services/purchase.py`
  منتقل شود تا ربات و API یک پیاده سازی داشته باشند. دو مسیر موازی
  برای پول یعنی اولین باگ مالی از همان جا می آید.
- **آپلود رسید.** از داخل مینی اپ سخت و بی ثمر است؛ در ربات می ماند.
- **تم روشن.** برند تیره است و عمدا تیره مانده، ولی رنگ هدر تلگرام
  با `setHeaderColor` ست می شود تا درز رنگی نبینی.

## نکته های اجرایی

- **اولین باز شدن کند است.** Passenger پروسه بی کار را می کشد.
  `keepalive` که داری کمک می کند؛ اگر باز هم کند بود،
  `KEEPALIVE_MINUTES` را کم کن.
- **مصرف ۴۵ ثانیه کش می شود.** بدون آن، هر بار برگشت به تب سرویس ها
  یک دور کامل درخواست به پنل می زد.
- **اگر پنل جواب ندهد،** به جای صفر، آخرین عکس روزانه نشان داده
  می شود و کنارش هشدار «عدد تازه نیست» می آید.
- **لاگ:** همه خطاهای مینی اپ زیر `obour.webapp` در `logs/obour.log`
  می نشینند. متن خطا هیچ وقت به کاربر نشان داده نمی شود.
