# تغییرات نسخه ۶.۱.۱ — کارت پلن

- **حجم هم وزن قیمت**: حجم قبلا در زیرعنوان 12 پیکسلی کنار روز گم بود، در
  حالی که قیمت 22 پیکسل بود. حالا حجم و قیمت دو خانه کنار هم اند و هر دو
  عدد درشت؛ خانه حجم فیروزه ای (رنگ اینترنت) است تا اول دیده شود.
- **دکمه خرید بی تردید**: قبلا دکمه سفید با مرز نازک روی کارت سفید بود و
  دیده نمی شد. حالا توپر فیروزه ای، با آیکن و پیکان. روی کارت «بهترین
  ارزش» سفید و درشت. رنگ فیروزه ای سیستم زیر متن سفید 3.78:1 بود؛ برای
  دکمه یک پله تیره تر (4.96:1) توکن شد.
- **کمبود موجودی** حالا کهربایی است، رنگ پول، و مستقیم صفحه شارژ را باز
  می کند.

---

# تغییرات نسخه ۶.۱.۰ — همه چیز داخل مینی اپ

## حالا داخل مینی اپ، بدون رفتن به ربات

- **شارژ کیف پول کامل**: انتخاب مبلغ (سه مبلغ آماده یا دلخواه) → مبلغ
  یکتا و شماره کارت با دکمه کپی → انتخاب عکس رسید → ارسال. رسید با همان
  کیبورد تایید/رد ربات برای ادمین ها می رود و تایید مثل قبل در ربات است.
  اگر کاربر وسط پرداخت مینی اپ را ببندد، با باز کردن دوباره همان مبلغ
  یکتا را می بیند نه یک مبلغ تازه.
  عکس روی گوشی کوچک می شود (حداکثر 1600 پیکسل، JPEG): آپلود روی اینترنت
  کند سریع تر است و اطلاعات جانبی عکس، مثل موقعیت مکانی، پاک می شود.
- **تیکت جدید و پاسخ** در خود گفتگو. ادمین مثل قبل با ریپلای جواب می دهد
  و پاسخ هم در مینی اپ و هم در ربات دیده می شود.
- **قوانین**: خواندن و پذیرفتن. اگر کاربر قبل از پذیرفتن قوانین خرید
  کند، مستقیم به صفحه قوانین می رود.
- **راهنمای اتصال**: دستگاه کاربر از خود تلگرام خوانده می شود و راهنمای
  همان دستگاه اول باز است. همان متن های ربات، با ارقام لاتین.

فقط دو جا هنوز ربات باز می شود و هر دو عمدی است: ثبت نام اولیه (عضویت
اجباری و قوانین در /start بررسی می شوند) و خرید هوش مصنوعی (نسخه بعد).

## رابط

- **کارت های سرویس اسلایدری**: با انگشت ورق می خورند. کارت کناری کمی
  عقب تر، چرخیده و کم رنگ تر است؛ نقطه ها و لرزش کوتاه جایگاه را نشان
  می دهند. روی اندروید ضعیف، چرخش خاموش است.
- **کارت لینک دعوت بازطراحی شد**: عنوان، لینک با دکمه کپی کنارش، و
  دکمه اشتراک تمام عرض.

## باگ ها

- **آیکن های غول پیکر روی کارت دعوت**: آیکن های داخل دکمه های کارت عبور
  اندازه نداشتند. به جای اصلاح همین یک مورد، یک قاعده سراسری اضافه شد
  که هر SVG بی اندازه را کوچک نگه می دارد — این سومین بار بود که این کلاس
  باگ پیدا شد.
- **رسید تکراری**: ارسال دوباره رسید روی همان تراکنش، دوباره برای ادمین ها
  می رفت. ربات این را با پاک کردن وضعیت گفتگو می بست؛ مینی اپ حالا صریح
  رد می کند.

## فایل های تازه

- `app/services/charge.py` — شارژ کیف پول، مستقل از رابط
- `app/services/support.py` — تیکت از مینی اپ

---

# تغییرات نسخه ۶.۰.۰ — طراحی کارت عبور

بازطراحی کامل رابط مینی اپ از روی سیستم طراحی «عبور — کارت عبور».
نسخه اصلی (MAJOR) بالا رفت چون ظاهر و ناوبری هر صفحه عوض شده.

## چه عوض شد

- **هر سرویس یک کارت عبور** — مثل کارت های کیف پول گوشی. سرویس اول
  سرمه ای زمینه لوگو، بعدی ها اقیانوسی، نیلی و آجری. چند سرویس روی هم
  چیده می شوند؛ زدن لبه یک کارت پشتی آن را جلو می آورد.
- **رنگ یعنی معنا**: فیروزه ای اینترنت، کهربایی پول، بنفش هوش مصنوعی،
  مرجانی پشتیبانی. آبی برند فقط برای اقدام اصلی — یکی در هر صفحه.
- **فقط تم روشن.** رنگ هدر و پس زمینه تلگرام همیشه روی مه روشن ما تنظیم
  می شود، حتی اگر تلگرام کاربر تیره باشد، تا درز رنگی نیفتد.
- **ناوبری چهار تب**: خانه، فروشگاه، کیف پول، حساب. «سرویس ها» حالا خود
  کارت های عبور در خانه است. نوار ناوبری شناور است.
- **سطح تخت، نه شیشه**: بلور، هاله و گرادیان پس زمینه حذف شدند. سایه فقط
  زیر کارت عبور، ناوبری و شیت.
- **فروشگاه**: به صرفه ترین پلن هر دسته خودش یک کارت عبور سرمه ای است و
  اول می آید؛ بقیه کارت سفید با قیمت هر گیگ و هر روز.
- **جزئیات سرویس**: کارت عبور، یک دکمه «وصل شوم» که شیت برنامه ها را باز
  می کند، کپی و QR، نمودار، اتصال سریع، جزئیات.
- **خانه کاربر تازه**: کارت خط چین «اولین کارت عبورت» و سه قدم تا اتصال.
- **هم سفرها**: لینک دعوت روی کارت سرمه ای، با اشتراک از پنجره بومی
  تلگرام (کاربر مستقیم مخاطب را انتخاب می کند).
- **حساب**: «افزودن به صفحه اصلی» (Bot API 8.0).
- **گفتگوی تیکت** تمام صفحه است: ناوبری کنار می رود و دکمه «پاسخ در ربات»
  جایش می نشیند.

## یک اصلاح کنتراست

رنگ متن ثانویه سیستم طراحی روی کارت اقیانوسی (4.1:1) و آجری (4.4:1)
زیر حد 4.5 بود. روی کارت های غیرسرمه ای رنگ روشن تری نشست (بالای 5.5).

## چه عوض نشد

سمت سرور: احراز هویت، API، خرید با nonce، سقف نرخ، QR، تمام صفحه، نمودار
یکنوا و کپی — منطق همه عینا از نسخه قبل آمده و تست های قبلی شان روی نسخه
جدید دوباره سبز شد.

---

# تغییرات نسخه ۵.۱۲.۰ — نمودار خطی و هدر تازه

## نمودار مصرف

جای نمودار میله ای، در جزئیات سرویس و صفحه خانه:

- خط نرم با گرادیان فیروزه ای به آبی — رنگ خود لوگوی عبور — و سطح
  محو شونده زیر آن.
- **درون یابی یکنوا (Fritsch–Carlson)**: منحنی های نرم معمولی بین دو
  نقطه بیش جهش دارند و روزی که مصرف صفر بوده، زیر صفر می روند (مصرف
  منفی نشان می دهند). این منحنی هرگز از بازه داده ها بیرون نمی زند. در
  تست، منحنی معمولی ۱۲ پیکسل زیر خط صفر رفت؛ این یکی دقیقا روی آن
  ایستاد.
- فقط بیشینه و کمینه برچسب می گیرند، با قرص و خط کوچک. وقتی همه روزها
  برابرند، یک برچسب.
- محور عمودی با گام گرد (2G، 4G، 6G) در واحد خودش — گیگ یا مگ.
- خط یک بار کشیده می شود؛ برای کسی که «حرکت کم» را در سیستم روشن کرده،
  بی انیمیشن.
- در جزئیات سرویس، بالای نمودار مجموع و میانگین روزانه.

## هدر

- دکمه انتقال به ربات حذف شد.
- موجودی حالا یک قرص شیشه ای با کاشی گرادیانی است، هم خانواده برند مارک
  سمت دیگر هدر: برچسب کوچک «موجودی» و مبلغ درشت. وقتی موجودی صفر است،
  کاشی یک + نشان می دهد و لبه آبی می گیرد: «شارژ کن».

---

# تغییرات نسخه ۵.۱۱.۰ — تمام صفحه

بر اساس مستند رسمی تلگرام (Bot API 8.0، بخش Full-screen Mode و
Design Guidelines).

- **تمام صفحه روی موبایل** (اندروید و iOS) با `requestFullscreen()`.
  روی دسکتاپ و وب تلگرام درخواست نمی شود: آنجا پنجره خودش اندازه دارد.
  روی تلگرام قدیمی تر از ۸.۰ همه چیز مثل قبل می ماند.
- **دو فاصله امن جدا**، همان طور که مستند تعریف کرده:
  - `safeAreaInset` — نوار وضعیت، ناچ و نوار ناوبری خود دستگاه
  - `contentSafeAreaInset` — دکمه های شناور خود تلگرام (بستن، منو)
  هدر زیر مجموع هر دو می نشیند، به علاوه یک فاصله نفس کشیدن. فاصله ها
  با رویدادهای `safeAreaChanged`، `contentSafeAreaChanged` و
  `fullscreenChanged` زنده به روز می شوند (مثلا با چرخش گوشی). فاصله های
  چپ و راست هم برای حالت افقی رعایت می شوند.
- پس زمینه هدر از لبه بالای صفحه شروع می شود، پس دکمه های شناور تلگرام
  روی شیشه مات هدر ما می نشینند نه روی محتوایی که زیرشان اسکرول می شود.
- **دکمه برگشت تکراری حذف شد.** در زیرصفحه ها هم دکمه برگشت بومی
  تلگرام دیده می شد و هم دکمه داخل صفحه. حالا فقط دکمه تلگرام.
- **اندرویدهای ضعیف**: تلگرام کلاس عملکرد دستگاه را در User-Agent
  می فرستد و راهنمای طراحی اش می گوید روی این دستگاه ها افکت ها را کم
  کنید. روی کلاس LOW، بلور پشت کارت ها و لایه نویز خاموش می شوند؛ ظاهر
  شیشه با گرادیان حفظ می شود.

---

# تغییرات نسخه ۵.۱۰.۱ — رفع سه باگ نمایشی

- **کارت های چسبیده.** دو کلاس پایه (`.stack` و `.between`) موقع
  بازنویسی شیشه ای گم شده بودند. کارت های تیکت و محصولات هوش مصنوعی
  بدون فاصله به هم چسبیده بودند و کد پیگیری تیکت به جای رفتن به آن سر،
  به برچسب وضعیت می چسبید.
- **شکل سفید بزرگ در تیکت تصویری.** آیکن «تصویر» بدون اندازه در متن
  می نشست و کل عرض کارت را می گرفت.
- **حباب های بلند و باریک در گفتگو.** `white-space:pre-wrap` روی کل
  حباب بود، پس فاصله ها و خط های خالی خود کد HTML به صورت خط خالی
  رندر می شدند. حالا فقط روی متن پیام است.

## بهبودهای همراه

- **گفتگو** مثل پیام رسان ها: ساعت کنار خط آخر، پیام های پشت سر هم یک
  طرف نزدیک به هم، دم حباب فقط روی آخرین پیام هر دسته، نام «پشتیبانی»
  بالای اولین پاسخ هر دسته، و جداکننده روز هر جا تاریخ عوض شد.
- **خانه برای کاربر بی سرویس**: کارت خوشامد با لوگو، سه ویژگی و دکمه
  پلن ها به جای جعبه خالی. اگر موجودی صفر باشد، پیشنهاد شارژ هم می آید.
  بخش های «مصرف روزهای اخیر» و «سایر سرویس ها» که فقط «داده ای نیست»
  می گفتند حذف می شوند.
- **موجودی تکراری** از سربرگ خانه برداشته شد (در هدر هست)؛ جایش تعداد
  سرویس های فعال و سلام بر اساس ساعت روز نشست.

## نگهبان تازه در /version

«همه کلاس های CSS تعریف شده اند» — هر کلاسی که در markup استفاده شده
ولی در CSS نیست را پیدا می کند. همان چیزی که این بار بی صدا ماند، دیگر
بی صدا نمی ماند.

---

# تغییرات نسخه ۵.۱۰.۰ — بازطراحی اجزای انتخاب

همه این اجزا از زبان بصری بقیه رابط بیرون می زدند: رنگ های تخت و
پرکنتراست کنار کارت های شیشه ای مات. حالا همه همان زبان شیشه را دارند.

- **برچسب های وضعیت** (فعال، رو به اتمام، منقضی، در انتظار، …): کپسول
  شیشه ای مات با لبه نور؛ رنگ فقط در یک نقطه نورانی می نشیند و متن
  آرام می ماند. فقط «فعال» نفس می کشد تا صفحه شلوغ نشود.
- **پرچم «بهترین ارزش»** جای برچسب «به صرفه ترین»: زبانه ای که از لبه
  بالای کارت آویزان است، با عنوان و برچسب ادمین (مثل «محبوب») رقابت
  نمی کند.
- **دسته های پلن** (روزانه، هفتگی، ماهانه): ریل شیشه ای با یک نشانگر
  که به گزینه فعال می لغزد، و زیر هر دسته تعداد پلن هایش. ایموجی دسته
  حذف شد - کنار ریل رنگی و ناهماهنگ بود. جابه جایی بین دسته ها دیگر
  به سرور درخواست نمی زند.
- **اینترنت آزاد / هوش مصنوعی** جای «کانفیگ / هوش مصنوعی»: دو کارت
  انتخاب با آیکن و یک خط توضیح. «کانفیگ» برای کاربر عادی واژه فنی
  بود و نمی گفت چه می خرد.
- **فیلتر کیف پول** (همه، شارژ، خرید، پاداش): همان ریل، با آیکن هایی که
  در ردیف تراکنش هم می آیند (پیکان پایین برای ورود پول، بالا برای خروج).
- بیش از چهار دسته در ریل جا نمی شود؛ آن وقت ریل خودکار اسکرولی می شود.

---

# تغییرات نسخه ۵.۹.۱ — رفع دو باگ

## ADMIN_KEY با علامت + در مسیرهای مدیریتی

`/setwebhook`، `/status`، `/cron`، `/setcommands` و `/delwebhook`
کلید را با `parse_qs` می خواندند، که طبق قاعده فرم های وب `+` را به
فاصله تبدیل می کند. کلید ADMIN_KEY معمولا base64 است و `+` دارد؛ پس
کلید با همان طول ولی بایت های متفاوت می رسید و بی صدا رد می شد.

حالا کلید از سه جا پذیرفته می شود: هدر `X-Admin-Key`، query خام (`+`
دست نخورده)، و query استاندارد. فاصله اضافه دور کلید هم نادیده گرفته
می شود. کلید اشتباه، حتی یک بایت متفاوت، همچنان رد می شود.

## لینک اتصال سریع در مینی اپ

در صفحه جزئیات سرویس، لینک های «اتصال سریع» دستی ساخته می شدند و نام
سرویس بدون encode در آدرس می نشست. نامی مثل «خانه & کار» به «خانه»
بریده می شد؛ `+` و `#` هم لینک را خراب می کردند. حالا از همان
`apps.import_link()` استفاده می شود که ربات استفاده می کند.

---

# تغییرات نسخه ۵.۹.۰ — گفتگوی کامل تیکت

- **فهرست تیکت ها** حالا کارت دارد: وضعیت، کد پیگیری، پیش نمایش
  پیام، تعداد پاسخ و تاریخ آخرین پیام.
- **گفتگوی کامل**: با زدن روی هر تیکت، همه پیام های آن رشته به شکل
  حباب پیام رسان نشان داده می شود - پیام کاربر آبی و سمت راست، پاسخ
  پشتیبانی خاکستری و سمت چپ. پیام تصویری با برچسب «تصویر» مشخص
  می شود (خود تصویر در ربات است؛ file_id تلگرام در صفحه وب قابل نمایش
  نیست).
- **پاسخ دادن در ربات می ماند.** دکمه «پاسخ در ربات» گفتگو را در ربات
  باز می کند. نوشتن تیکت از مینی اپ یعنی ساختن یک مسیر اعلان دوم به
  ادمین ها، و مسیر فعلی ربات آزموده است.
- **خوانده شدن**: باز کردن فهرست تیکت ها در مینی اپ، پاسخ ها را خوانده
  شده علامت می زند - همان کاری که ربات می کند.
- **مالکیت**: هر تیکت فقط برای صاحبش باز می شود؛ برای بقیه ۴۰۴.
- **ناوبری پشته ای**: «برگشت» از گفتگو حالا به فهرست تیکت ها برمی گردد،
  نه یکراست به تب اصلی. تعویض تب کل پشته را پاک می کند.

---

# تغییرات نسخه ۵.۸.۰ — فونت پیدا

- فونت **پیدا** جای فونت های قبلی نشست، با چهار وزن: ۴۰۰ برای متن،
  ۶۰۰ برای عنوان و دکمه، ۷۰۰ و ۸۰۰ برای عدد.
- وزن های Thin، ExtraLight، Light، Medium و Black عمدا نیامدند. طراحی
  از آن ها استفاده نمی کند و هر وزن حدود ۲۷ کیلوبایت است؛ روی اینترنت
  موبایل ایران هر فایل اضافه یعنی چند صد میلی ثانیه تاخیر تا متن با
  فونت درست بنشیند.
- **اعداد** حالا از همین فونت اند و وزن ۸۰۰ دارند. عدد در این رابط
  همیشه مهم ترین چیز سطر است (مبلغ، حجم، روز مانده) و باید از متن
  فارسی کنارش جدا شود.
- وزن ۴۰۰ و ۸۰۰ با `preload` بارگذاری می شوند، چون اولین چیزی که
  کاربر می بیند متن و عدد است.
- `/wafont` سر جایش است: اگر فونت دیگری آپلود کنی، بر پیدا غالب می شود.
- مسیر `‌/api/installfont` حذف شد. وقتی فونت همراه خود پروژه می آید،
  گرفتنش از گوگل فقط یک وابستگی اضافه است.

---

# تغییرات نسخه ۵.۷.۰ — رابط کاربری مینی اپ

## تازه در ۵.۷.۰

- **کپی درست شد.** در وب ویو تلگرام، `navigator.clipboard` اغلب رد
  می شود و چون Promise است، وقتی reject می شود «ژست کاربر» تمام شده و
  راه جایگزین هم دیگر مجاز نیست. حالا اول روش همزمان امتحان می شود و
  اگر هر دو شکست خوردند، متن برای کپی دستی نشان داده می شود.
- **تم روشن خوانا شد.** نوار پایین، چیپ ها و سگمنت ها متن سفید داشتند
  که روی شیشه روشن ناپدید می شد. حالا رنگشان توکن دارد.
- **موجودی در هدر** - هر تصمیم خرید به آن بستگی دارد و نباید برای
  دیدنش تب عوض شود. بعد از خرید با یک تاباندن کوتاه به روز می شود.
- **فروشگاه دوبخشی**: کانفیگ و هوش مصنوعی.
- **کارت پلن پرجزئیات تر**: قیمت هر گیگ، قیمت هر روز، حجم روزانه، و
  علامت خوردن پلن به صرفه ترین. اگر موجودی کم باشد، به جای دکمه خرید
  کسری نشان داده می شود.
- **بخش هوش مصنوعی** با بنر ۱۶:۹، فهرست محصولات و سفارش های قبلی.
  سفارش نهایی عمدا در ربات می ماند: قیمتش دلاری است و به موجودی لحظه ای
  فروشنده بستگی دارد؛ مسیر پرداخت دوم برایش ارزش ریسک ندارد.
- **دو دستور تازه ادمین**:
  - `/wafont` — فایل فونت را به صورت سند بفرست و روی آن ریپلای کن.
    `/wafont off` برای برداشتن.
  - `/wabanner` — روی یک عکس ۱۶:۹ ریپلای کن. `/wabanner off` برای
    برداشتن.

---

# تغییرات نسخه ۵.۶.۰ — خرید از مینی اپ

> از این نسخه شماره گذاری به صورت `MAJOR.MINOR.PATCH` است:
> رفع باگ ← `5.5.1` · قابلیت تازه ← `5.6.0` · تغییر بزرگ ← `6.0.0`
> نسخه های قبلی (۵۴ و ۵۵) همان ۵.۴ و ۵.۵ هستند.

مینی اپ **مکمل** ربات است، نه جایگزین. همه دستورها، منوها و دکمه های
قبلی دست نخورده اند.

در این نسخه **خرید سرویس** از مینی اپ هم ممکن شد. خرید از همان
`app/services/purchase.py` رد می شود که ربات استفاده می کند، پس یک
پیاده سازی بیشتر برای پول وجود ندارد. شارژ، تمدید و تیکت همچنان در
ربات انجام می شوند.

`POST /api/purchase` تنها مسیر نوشتنی است. بقیه مسیرها با فهرست سفید
خواندنی می مانند، تا اندپوینت تازه ای بی سروصدا قابل نوشتن نشود.

---

## فایل های تازه

```
app/webapp/__init__.py          آدرس مینی اپ + نصب فونت از گوگل
app/webapp/auth.py              احراز هویت initData تلگرام
app/webapp/api.py               اندپوینت های خواندنی + QR ساده
app/webapp/wsgi.py              پل بین Passenger و توابع async
app/webapp/static/index.html    خود مینی اپ (یک فایل، بدون CDN)
app/webapp/static/logo.webp     لوگوی عبور
app/services/__init__.py       لایه منطق مستقل از رابط
app/services/purchase.py       خرید مشترک ربات و مینی اپ
WEBAPP_FA.md                    راهنمای نصب
```

## فایل های تغییر کرده

```
passenger_wsgi.py   مسیر /app + ثبت خودکار دکمه منوی چت
app/keyboards.py    دکمه «مینی اپ عبور» در منوی اصلی
app/emoji.py        کلید webapp در کاتالوگ ایموجی
app/version.py      نسخه ۵.۵.۰ + چک های سلامت
app/handlers/buy.py منطق خرید به سرویس لایر منتقل شد (رفتار بدون تغییر)
app/handlers/extras.py  به _create_with_retry در سرویس جدید وصل شد
```

---

## نصب

### ۱) env

```ini
WEBHOOK_BASE_URL=https://dezhcode.pyho.ir/obour
APP_BASE_URI=/obour
ADMIN_IDS=789912945          # برای عیب یابی و نصب فونت

WEBAPP_PATH=app              # اختیاری
WEBAPP_ENABLED=true          # اختیاری
WEBAPP_MAX_AGE=21600         # اختیاری - عمر نشست به ثانیه
```

اگر `WEBHOOK_BASE_URL` با `https://` شروع نشود، مینی اپ خودش را خاموش
می کند و دکمه اش ساخته نمی شود — تلگرام مینی اپ روی http را باز نمی کند.

### ۲) آپلود و Restart

کل پوشه را جایگزین کن و در سی پنل **Restart** بزن.

### ۳) یک بار این را بزن

```
https://dezhcode.pyho.ir/obour/setwebhook?key=ADMIN_KEY
```

سه کار می کند: وبهوک، فهرست دستورها، و دکمه منوی چت که به مینی اپ وصل
می شود. خروجی باید این شکلی باشد:

```
webhook set: https://dezhcode.pyho.ir/obour/webhook/obour
webapp: https://dezhcode.pyho.ir/obour/app
```

### ۴) فونت

کاری لازم نیست. فونت پیدا همراه پروژه می آید و در
`app/webapp/static/fonts/` نشسته است. اگر فونت دیگری خواستی، با
`/wafont` در ربات آپلود کن؛ بر پیدا غالب می شود.

### ۵) تایید نصب

در ربات `/version` بزن. باید نسخه ۶.۱.۱ و همه موارد ✅ باشند. هر ❌ یعنی
آن فایل جایگزین نشده.

---

## آدرس ها

| مسیر | کار |
|---|---|
| `/obour/app/` | خود مینی اپ |
| `/obour/app/api/bootstrap` | کاربر، موجودی، شمارنده ها |
| `/obour/app/api/services` | سرویس ها با مصرف زنده |
| `/obour/app/api/service?id=N` | جزئیات + لینک ساب + اتصال سریع |
| `/obour/app/api/qr?id=N` | QR ساده (SVG) |
| `/obour/app/api/wallet?offset=0&kind=charge` | سوابق مالی |
| `/obour/app/api/plans` | ویترین فروشگاه |
| `/obour/app/api/referral` | هم سفرها |
| `/obour/app/api/tickets` | فهرست تیکت ها |
| `/obour/app/api/ticket?id=N` | گفتگوی کامل یک تیکت |
| `POST /obour/app/api/purchase` | خرید سرویس (تنها مسیر نوشتنی) |
| `/obour/app/api/diag` | عیب یابی (فقط ادمین) |
| `/obour/app/api/installfont` | نصب فونت (فقط ادمین) |

---

## امنیت

- هر درخواست API رشته `initData` را در هدر `X-Init-Data` می آورد و
  امضایش با HMAC-SHA256 سنجیده می شود. هدر انتخاب شده نه query string،
  چون query string در access log هاست ذخیره می شود.
- امضا با **هر چهار حالت ممکن** سنجیده می شود: دو نوع تجزیه × با و
  بدون فیلد `signature`. تلگرام از ۲۰۲۴ فیلد `signature` را هم
  می فرستد و مستندات و پیاده سازی های واقعی سر آوردنش در محاسبه
  اختلاف دارند. اگر اشتباه انتخاب شود، ورود برای همه کاربران می شکند
  و از بیرون شبیه «توکن غلط» دیده می شود.
- `auth_date` قدیمی تر از `WEBAPP_MAX_AGE` رد می شود.
- `service?id=` و `qr?id=` مالکیت را چک می کنند. بدون آن هر کسی با
  شماره گذاری ساده لینک ساب دیگران را می خواند.
- هر متدی جز GET با ۴۰۵ رد می شود.
- سقف نرخ: ۶۰ درخواست در دقیقه به ازای هر کاربر.
- CSP فقط منابع خودی؛ `frame-ancestors` فقط دامنه های تلگرام (نسخه وب
  تلگرام مینی اپ را در iframe باز می کند، پس `X-Frame-Options: DENY`
  آن را می شکند).
- هر متن سروری از `esc()` رد می شود.
- اگر کاربر `/start` نزده باشد به ربات فرستاده می شود؛ پذیرش قوانین و
  عضویت اجباری دور زده نمی شوند.

---

## کارهایی که این نسخه عمدا نمی کند

- **شارژ کیف پول از مینی اپ.** هنوز در ربات است؛ آپلود رسید از داخل
  مینی اپ سخت و بی ثمر است. قدم بعدی می تواند ساخت درخواست شارژ با
  مبلغ یکتا باشد، با رسید همچنان در ربات.
- **کد تخفیف در مینی اپ.** خرید از مینی اپ فعلا بدون کد تخفیف است.
  اعتبارسنجی کد در ربات به وضعیت گفتگو گره خورده و برای مینی اپ باید
  جدا نوشته شود.
- **آپلود رسید.** در ربات می ماند.

---

## نکته های اجرایی

- اولین باز شدن کند است؛ Passenger پروسه بی کار را می کشد. اسپلش تا
  آمدن داده می ماند، نه یک تاخیر تزئینی.
- مصرف ۴۵ ثانیه کش می شود، وگرنه هر بار برگشت به تب سرویس ها یک دور
  کامل درخواست به پنل می زد.
- اگر پنل جواب ندهد، آخرین عکس روزانه با برچسب «عدد تازه نیست» نشان
  داده می شود، نه صفر.
- لاگ مینی اپ زیر `obour.webapp` در `logs/obour.log`.
- دکمه مینی اپ از سیستم ایموجی رد می شود (کلید `webapp`)، پس در پنل
  ادمین قابل تغییر است و ایموجی پریمیوم هم می پذیرد.
