# نسخه ۵۴ — پیگیری سفارش، ایموجی جدا، بدون خاموشی گیج کننده

---

## ۱) «سرویس های من» یا سفارش جدا؟

جواب کوتاه: **جدا می ماند**، و دلیلش دارد. «سرویس های من» برای
کانفیگ هاست - چیزی با حجم و تاریخ انقضا که وضعیتش هر روز عوض می شود.
اشتراک جمنای یک **لینک یک بار مصرف** است؛ قاطیشان کردن هم برای کاربر
گیج کننده می شد هم برای کد.

به جایش، سفارش هوش مصنوعی از **دو راه** پیگیری می شود:

- **📚 سفارش های من** (داخل صفحه خدمات هوش مصنوعی) - فهرست کامل
- **`/track AI-xxxxx`** - دقیقا مثل کد شارژ و تیکت، حالا کدهای
  `AI-` هم شناخته می شوند

## ۲) کد سفارش نزد سرویس دهنده + وضعیت لحظه ای

هم پیام تحویل و هم صفحه پیگیری، حالا **کد سفارش warzoneshop**
(`ORD-04621-9a84`) را جدا از کد داخلی ما نشان می دهند:

```
🔎 کد پیگیری ما
├ AI-04F2

کد سفارش نزد سرویس دهنده
╯─ ORD-04621-9a84
```

روی صفحه پیگیری دکمه **🔃 وضعیت لحظه ای** هست که مستقیم تاریخچه
سرویس دهنده را می گردد و آخرین وضعیت را نشان می دهد.

*صادقانه بگویم:* سرویس دهنده اندپوینت «یک سفارش» جدا ندارد، فقط
تاریخچه صفحه بندی شده. این دکمه صفحه به صفحه دنبال کد می گردد -
معمولا سریع است چون سفارش های تازه اول فهرست هستند.

## ۳) 🎯 خاموشی گیج کننده حذف شد

حق با تو بود. قبلا وقتی موجودی سرویس دهنده تمام می شد، کل بخش خدمات
هوش مصنوعی از منو **ناپدید** می شد - کاربر هیچ توضیحی نمی دید، فقط
دکمه دیگر نبود.

**حالا:** بخش همیشه در دسترس می ماند. اگر واقعا ناموجود بود، پیام
روشن می بینی:

```
💎 جمنای پرو ۱۸ ماهه
فعلا موجود نیست

این سرویس همین الان تموم شده. بعد از موجود شدن خودمون
خبرت می کنیم - لازم نیست هر چند دقیقه سر بزنی.

[🔔 وقتی موجود شد خبرم کن]
```

با زدن دکمه، کاربر در **فهرست انتظار** ثبت می شود. وقتی موجودی را
شارژ کردی، از پنل ← 🤖 خدمات هوش مصنوعی ← **🔔 موجود شد، خبردار کن**
یک پیام به همه آن ها می رود و فهرست خالی می شود.

عمدا این اطلاع رسانی **دستی** است، نه خودکار: بعد از یک بار قطعی،
اعتماد به «الان مثبت است» فقط با تایید خودت معنا دارد.

## ۴) درباره موجودی: قبلا هم مال خودت بود

خیالت راحت باشد - `/me` که موجودی از آن خوانده می شود، **به کلید
API تو گره خورده**، نه یک عدد کلی پلتفرم. یعنی همانی که در پنل
می بینی، دقیقا موجودی حساب خودت نزد warzoneshop است.

## ۵) ایموجی های جدا برای هرچه در متن محصول بود

پنج کلید تازه، کاتالوگ به **۱۲۹** رسید:

- `gemini` 💎 - عنوان محصول (جدا از 🤖 که برای منوی خدمات هوش
  مصنوعی می ماند)
- `warranty` 🎖 - چون 🛡 قبلا برای آیکون بخش ادمین رزرو بود؛
  استفاده از همان باعث می شد سفارشی کردن آن، این خط را هم عوض کند
- `check` ✔️ - جدا از ✅ که برای «تایید» عمومی استفاده می شود
- `country_pin` 📍
- `sync` 🔃 - دکمه وضعیت لحظه ای

کلید `family` هم از 👨‍👩‍👧 (سه نفره) به 👨‍👩‍👧‍👦 (چهار نفره) عوض
شد - دقیقا همانی که فرستادی.

## ۶) متن محصول - کمتر فاصله

دو کادر نقل قول جدا (با یک خط خالی بینشان) در **یک کادر** ادغام شد.
همان محتوا، فاصله بصری کمتر:

```
✔️ اشتراک اصلی و کامل روی جیمیل خودت فعال می شه
☁️ ۵ ترابایت فضای ابری گوگل
👨‍👩‍👧‍👦 اکانت خانوادگی - تا ۵ نفر می تونی اضافه کنی
🔒 کاملا شخصی، اشتراکی نیست
📍 توی همه کشورها کار می کنه، بدون تایید هویت
💳 نیازی به کارت بانکی خارجی نداری
🎖 ۲۴ ساعت گارانتی

┃ بعد از پرداخت یه لینک فعال سازی می گیری. توی مرورگر
┃ بازش کن و روی «Activate Offer» بزن - همین.
┃ ⚠️ لینک یک بار مصرفه و بعد از صدور برگشت نمی خوره؛
┃ قبل از خرید مطمئن شو جیمیلت آماده ست.

💰 قیمت: 95,000 تومان
╯─ موجودی تو: 120,000 تومان
```

---

## نصب

کل پوشه `app/` را جایگزین کن، بعد `./restart.sh`.
جدول `ai_waitlist` خودش ساخته می شود.

## بعد از دیپلوی

۱. تنظیمات ← 🎨 ایموجی ها ← خرید — پنج کلید تازه را ببین
۲. یک خرید آزمایشی بزن، بعد `/track AI-xxxxx` را با کدی که گرفتی
   امتحان کن
۳. اگر موجودی صفر بود، دکمه «خبرم کن» را بزن و بعد از شارژ، از پنل
   «موجود شد، خبردار کن» را امتحان کن
